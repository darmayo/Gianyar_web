import { AdminDashboard } from './AdminDashboard'

export const metadata = {
  title: 'Admin Pengajuan | Kabupaten Gianyar',
  robots: { index: false, follow: false },
}

export default function AdminPage() {
  return <AdminDashboard />
}
